# Context patch for the tested production-3.73-vision image.
# Apply only after confirming the surrounding code matches.
# This is a local workaround, not an upstream fix.

--- speculative.py.stock
+++ speculative.py
@@
-        elif (
-            self.num_speculative_tokens > n_predict
-            and self.num_speculative_tokens % n_predict != 0
-        ):
+        elif (
+            self.method != "dspark"
+            and self.num_speculative_tokens > n_predict
+            and self.num_speculative_tokens % n_predict != 0
+        ):
